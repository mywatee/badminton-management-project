<Page x:Class="QuanLySCL.GUI.Views.PromotionsAdminView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      Title="Quản lý Khuyến mãi">

    <Grid Background="{StaticResource BackgroundGray}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="32">
                <!-- Header -->
                <Grid Margin="0,0,0,32">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="Quản lý mã Khuyến mãi (Voucher)" FontSize="28" FontWeight="Bold" Foreground="{StaticResource NavyBlueBrush}"/>
                        <TextBlock Text="Tạo và thiết lập các mã giảm giá cho khách hàng." FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                        <Button Click="Reload_Click" ToolTip="Tải lại dữ liệu"
                                Style="{StaticResource MaterialDesignIconButton}" Foreground="#64748B" Margin="0,0,8,0">
                            <materialDesign:PackIcon Kind="Refresh" />
                        </Button>
                        <Button Click="Add_Click" Content="TẠO MÃ MỚI" 
                                Style="{StaticResource MaterialDesignFlatButton}"
                                Background="{StaticResource LimeGreen}" Foreground="#0F172A" FontWeight="Bold" Height="40" Padding="16,0">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>
                </Grid>

                <!-- DataGrid -->
                <Border Style="{StaticResource Card}" Padding="0" ClipToBounds="True">
                    <DataGrid ItemsSource="{Binding Promotions}"
                              SelectedItem="{Binding SelectedPromotion}"
                              AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              GridLinesVisibility="None" HeadersVisibility="Column" Background="White" BorderThickness="0"
                              Style="{StaticResource MaterialDesignDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="MÃ KM" Binding="{Binding MaKM}" FontWeight="Bold" Foreground="#4F46E5" Width="120"/>
                            <DataGridTextColumn Header="TÊN CHƯƠNG TRÌNH" Binding="{Binding TenKM}" Width="250"/>
                            
                            <!-- Kieu Giam gia -->
                            <DataGridTemplateColumn Header="LOẠI GIẢM" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="4" Padding="6,2" HorizontalAlignment="Left" Background="#EEF2FF">
                                            <TextBlock Text="{Binding Kieu}" FontSize="12" FontWeight="SemiBold" Foreground="#4F46E5"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="MỨC GIẢM" Binding="{Binding GiaTri, StringFormat=N0}" FontWeight="SemiBold" Width="120"/>
                            <DataGridTextColumn Header="ĐƠN TỐI THIỂU" Binding="{Binding DonToiThieu, StringFormat=N0}" Foreground="#64748B" Width="150"/>
                            
                            <DataGridTemplateColumn Header="TRẠNG THÁI" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="4" Padding="6,2" HorizontalAlignment="Left">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#DCFCE7"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding TrangThai}" Value="False">
                                                            <Setter Property="Background" Value="#FEE2E2"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock FontSize="12" FontWeight="SemiBold">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text" Value="Hoạt động"/>
                                                        <Setter Property="Foreground" Value="#15803D"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding TrangThai}" Value="False">
                                                                <Setter Property="Text" Value="Ngừng"/>
                                                                <Setter Property="Foreground" Value="#B91C1C"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="THAO TÁC" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Click="Edit_Click" ToolTip="Sửa" Style="{StaticResource MaterialDesignIconButton}" Width="32" Height="32">
                                                <materialDesign:PackIcon Kind="PencilOutline" Width="16" Height="16" Foreground="#64748B"/>
                                            </Button>
                                            <Button Click="Delete_Click" ToolTip="Xóa" Style="{StaticResource MaterialDesignIconButton}" Width="32" Height="32" Margin="4,0,0,0">
                                                <materialDesign:PackIcon Kind="DeleteOutline" Width="16" Height="16" Foreground="#F87171"/>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
