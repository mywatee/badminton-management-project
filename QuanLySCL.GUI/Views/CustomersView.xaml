<Page x:Class="QuanLySCL.GUI.Views.CustomersView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Khách hàng">

    <!--
        Performance notes:
        - Do NOT wrap DataGrid in an outer ScrollViewer/StackPanel: it disables virtualization and causes lag.
        - Use Grid with star row so DataGrid gets a finite height.
    -->
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,32">
            <StackPanel>
                <TextBlock Text="Quản lý khách hàng" FontSize="32" FontWeight="Bold" Foreground="#0F172A"/>
                <TextBlock Text="Quản lý thông tin khách hàng và lịch sử đặt sân" FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
            </StackPanel>

            <Button HorizontalAlignment="Right" VerticalAlignment="Center" Style="{StaticResource LimeButton}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Plus" Width="16" Height="16" Margin="0,0,8,0"/>
                    <TextBlock Text="Thêm khách hàng"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Stats cards -->
        <Grid Grid.Row="1" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,12,0">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Tổng số khách hàng" FontSize="13" Foreground="#64748B"/>
                        <TextBlock Text="{Binding Customers.Count}" FontSize="24" FontWeight="Bold" Foreground="#0F172A" Margin="0,8,0,0"/>
                    </StackPanel>
                    <Border Width="48" Height="48" Background="#3B82F6" CornerRadius="8" HorizontalAlignment="Right" VerticalAlignment="Top">
                        <materialDesign:PackIcon Kind="AccountGroup" Width="24" Height="24" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource Card}" Margin="6,0">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Khách VIP" FontSize="13" Foreground="#64748B"/>
                        <TextBlock Text="{Binding VipCustomers}" FontSize="24" FontWeight="Bold" Foreground="#0F172A" Margin="0,8,0,0"/>
                    </StackPanel>
                    <Border Width="48" Height="48" Background="#8B5CF6" CornerRadius="8" HorizontalAlignment="Right" VerticalAlignment="Top">
                        <materialDesign:PackIcon Kind="Award" Width="24" Height="24" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource Card}" Margin="6,0">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Khách mới trong tháng" FontSize="13" Foreground="#64748B"/>
                        <TextBlock Text="{Binding NewCustomersThisMonth}" FontSize="24" FontWeight="Bold" Foreground="#0F172A" Margin="0,8,0,0"/>
                    </StackPanel>
                    <Border Width="48" Height="48" Background="#84CC16" CornerRadius="8" HorizontalAlignment="Right" VerticalAlignment="Top">
                        <materialDesign:PackIcon Kind="AccountPlus" Width="24" Height="24" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="3" Style="{StaticResource Card}" Margin="12,0,0,0">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Hoạt động hôm nay" FontSize="13" Foreground="#64748B"/>
                        <TextBlock Text="{Binding ActiveToday}" FontSize="24" FontWeight="Bold" Foreground="#0F172A" Margin="0,8,0,0"/>
                    </StackPanel>
                    <Border Width="48" Height="48" Background="#10B981" CornerRadius="8" HorizontalAlignment="Right" VerticalAlignment="Top">
                        <materialDesign:PackIcon Kind="CalendarCheck" Width="24" Height="24" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <!-- List -->
        <Border Grid.Row="2" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <TextBlock Text="Danh sách khách hàng" FontSize="20" FontWeight="SemiBold" Foreground="#0F172A"/>
                    <TextBox HorizontalAlignment="Right"
                             Width="320"
                             materialDesign:HintAssist.Hint="Tìm theo tên, số điện thoại, email hoặc mã..."
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding VisibleCustomers}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          SelectionMode="Single"
                          AlternatingRowBackground="#F8FAFC"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling"
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.IsDeferredScrollingEnabled="True"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="90"/>
                        <DataGridTextColumn Header="Họ tên" Binding="{Binding Name}" Width="200"/>

                        <DataGridTemplateColumn Header="Liên hệ" Width="240">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Phone}" Foreground="#0F172A" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Email}" Foreground="#64748B" FontSize="12"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Tổng số lượt đặt" Binding="{Binding TotalBookings}" Width="120"/>
                        <DataGridTextColumn Header="Tổng chi tiêu" Binding="{Binding TotalSpent, StringFormat='₫{0:N0}'}" Width="120"/>
                        <DataGridTextColumn Header="Thành viên từ" Binding="{Binding MemberSince, StringFormat='yyyy-MM-dd'}" Width="120"/>

                        <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="90"/>

                        <DataGridTemplateColumn Header="Thao tác" Width="130">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Xem chi tiết"
                                            MinWidth="110"
                                            Padding="10,6"
                                            BorderThickness="1"
                                            BorderBrush="#CBD5E1"
                                            Background="White"
                                            Foreground="#0F172A"
                                            HorizontalAlignment="Stretch"
                                            Click="ViewCustomerDetails_Click"
                                            Tag="{Binding Id}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <Button Grid.Row="2"
                        Content="Xem thêm"
                        Margin="0,14,0,0"
                        Padding="16,10"
                        HorizontalAlignment="Center"
                        Background="#E2E8F0"
                        BorderBrush="#CBD5E1"
                        Foreground="#0F172A"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Click="LoadMore_Click">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CanLoadMore}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Page>
