<Window x:Class="QuanLySCL.GUI.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Đăng Nhập" Height="550" Width="400"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border CornerRadius="20" Background="White" BorderBrush="#E2E8F0" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.1"/>
        </Border.Effect>

        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,30">
                <Border Width="60" Height="60" Background="#0984e3" CornerRadius="15">
                    <md:PackIcon Kind="Badminton" Width="32" Height="32" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Border>
                <TextBlock Text="THE CHAMPIONS" FontSize="22" FontWeight="Bold" Foreground="#0F172A" HorizontalAlignment="Center" Margin="0,15,0,5"/>
                <TextBlock Text="Quản lý sân cầu lông" Foreground="#64748B" HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Form -->
            <StackPanel Grid.Row="1">
                <!-- Alert (inline so it won't cover inputs) -->
                <Border x:Name="brdAlert" Background="#FEE2E2" CornerRadius="8" Padding="10" Margin="0,0,0,12" Visibility="Collapsed">
                    <TextBlock x:Name="txtAlert" Foreground="#EF4444" FontSize="12" TextWrapping="Wrap"/>
                </Border>
                <TextBlock Text="Tài khoản" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,5"/>
                <TextBox x:Name="txtUser" Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                          md:HintAssist.Hint="Nhập tên đăng nhập" FontSize="14" Margin="0,0,0,20"/>

                <!-- Phần nhập Mật khẩu -->
                <TextBlock Text="Mật khẩu" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <PasswordBox x:Name="txtPassword" Height="35" Margin="0,0,0,5" VerticalContentAlignment="Center" Padding="10,0" BorderBrush="#dfe6e9"/>

                <!-- Phần Quên mật khẩu -->
                <TextBlock HorizontalAlignment="Right" Margin="0,0,0,20">
                    <Hyperlink Click="linkForgotPass_Click" 
                               Foreground="#636e72" 
                               FontSize="11" 
                               TextDecorations="None">
                        Quên mật khẩu?
                    </Hyperlink>
                </TextBlock>

                <Button Content="ĐĂNG NHẬP" Background="#0984e3" Foreground="White" FontWeight="Bold" Height="45" 
                        Click="Login_Click" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>

            <!-- Footer -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <TextBlock Foreground="#64748B">
                    <Run Text="Chưa có tài khoản?   "/>
                    <Hyperlink Click="Register_Click" 
                               Foreground="#0984e3" 
                               FontWeight="Bold" 
                               TextDecorations="None">
                        Đăng ký ngay
                    </Hyperlink>
                </TextBlock>
            </StackPanel>

            <!-- Nút đóng -->
            <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="-15,-15,-15,0"
                    Width="30" Height="30" Background="Transparent" BorderThickness="0"
                    Foreground="Black" FontSize="14" Cursor="Hand" Click="btnClose_Click" Panel.ZIndex="10">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="15">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FEE2E2"/>
                                <Setter Property="Foreground" Value="#EF4444"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>
