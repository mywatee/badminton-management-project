<Window x:Class="QuanLySCL.GUI.Windows.CourtDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Chi tiết sân"
        Height="640"
        Width="920"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundGray}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="18" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="{Binding Court.Name}" FontSize="22" FontWeight="Bold" Foreground="#0F172A"/>
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <TextBlock Text="{Binding Court.Id}" Foreground="#64748B" FontWeight="SemiBold" Margin="0,0,10,0"/>
                        <TextBlock Text="•" Foreground="#CBD5E1" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding Court.Type}" Foreground="#64748B" Margin="0,0,10,0"/>
                        <TextBlock Text="•" Foreground="#CBD5E1" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding Court.Status}" Foreground="#64748B"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                    <TextBlock Text="Ngày:" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <DatePicker SelectedDate="{Binding SelectedDate, UpdateSourceTrigger=PropertyChanged}"
                                Width="150"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="18">
            <StackPanel>
                <Grid Margin="0,0,0,12">
                    <TextBlock Text="Lịch đặt trong ngày" FontSize="16" FontWeight="SemiBold" Foreground="#0F172A"/>
                    <Button Content="Đóng" HorizontalAlignment="Right" Padding="12,6" Background="#E2E8F0" BorderBrush="#CBD5E1" Foreground="#0F172A" Cursor="Hand" Click="Close_Click"/>
                </Grid>

                <DataGrid ItemsSource="{Binding Bookings}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#F8FAFC"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Giờ" Binding="{Binding TimeLabel}" Width="Auto"/>
                        <DataGridTextColumn Header="Khách" Binding="{Binding Customer}" Width="200"/>
                        <DataGridTextColumn Header="SĐT" Binding="{Binding Phone}" Width="Auto"/>
                        <DataGridTextColumn Header="Loại" Binding="{Binding Type}" Width="Auto"/>
                        <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="Auto"/>
                        <DataGridTextColumn Header="Số tiền" Binding="{Binding Amount, StringFormat='₫{0:N0}'}" Width="Auto"/>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Text="Không có lịch đặt cho ngày này."
                           Foreground="#64748B"
                           Margin="0,12,0,0"
                           HorizontalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Bookings.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</Window>

