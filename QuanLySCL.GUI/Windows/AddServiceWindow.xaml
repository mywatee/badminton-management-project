<Window x:Class="QuanLySCL.GUI.Windows.AddServiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Thêm dịch vụ" Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundGray}"
        WindowStyle="None"
        AllowsTransparency="True">
    
    <Border BorderBrush="{StaticResource NavyBlueBrush}" BorderThickness="1" CornerRadius="12" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{StaticResource NavyBlueBrush}" CornerRadius="11,11,0,0" Padding="20,15">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="PlusCircle" Foreground="{StaticResource LimeGreen}" Width="24" Height="24" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock Text="THÊM DỊCH VỤ VÀO SÂN" Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" Style="{StaticResource MaterialDesignIconButton}" Foreground="White" Click="Cancel_Click">
                        <materialDesign:PackIcon Kind="Close" Width="24" Height="24"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <Grid Grid.Row="1" Margin="24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>

                <!-- Service List -->
                <StackPanel Grid.Column="0" Margin="0,0,16,0">
                    <TextBlock Text="Chọn dịch vụ" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <RadioButton Content="Tất cả" IsChecked="True" Style="{StaticResource MaterialDesignTabRadioButton}" Margin="0,0,8,0" Command="{Binding FilterCommand}" CommandParameter="All"/>
                        <RadioButton Content="Đồ uống" Style="{StaticResource MaterialDesignTabRadioButton}" Margin="0,0,8,0" Command="{Binding FilterCommand}" CommandParameter="Drinks"/>
                        <RadioButton Content="Dụng cụ" Style="{StaticResource MaterialDesignTabRadioButton}" Command="{Binding FilterCommand}" CommandParameter="Equipment"/>
                    </StackPanel>

                    <ListBox x:Name="ServiceList" ItemsSource="{Binding FilteredServices}" 
                             SelectedItem="{Binding SelectedService}"
                             Height="280">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontWeight="Medium" FontSize="14"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{Binding Price, StringFormat={}{0:N0}đ}" 
                                               HorizontalAlignment="Right" VerticalAlignment="Center" 
                                               Foreground="{StaticResource NavyBlueBrush}" FontWeight="SemiBold"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>

                <!-- Selection Details -->
                <StackPanel Grid.Column="1" VerticalAlignment="Top">
                    <Border Background="#F1F5F9" Padding="16" CornerRadius="8">
                        <StackPanel>
                            <TextBlock Text="Số lượng" FontWeight="SemiBold" Margin="0,0,0,12"/>
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="0" Style="{StaticResource MaterialDesignOutlinedButton}" Padding="0" Width="36" Height="36" Command="{Binding DecreaseCommand}">
                                    <materialDesign:PackIcon Kind="Minus" Width="18"/>
                                </Button>
                                <TextBox Grid.Column="1" Text="{Binding Quantity}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="18" FontWeight="Bold" BorderThickness="0" Background="Transparent"/>
                                <Button Grid.Column="2" Style="{StaticResource MaterialDesignOutlinedButton}" Padding="0" Width="36" Height="36" Command="{Binding IncreaseCommand}">
                                    <materialDesign:PackIcon Kind="Plus" Width="18"/>
                                </Button>
                            </Grid>

                            <TextBlock Text="Thành tiền dự kiến" FontSize="11" Foreground="Gray"/>
                            <TextBlock Text="{Binding TotalPrice, StringFormat={}{0:N0}đ}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource NavyBlueBrush}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- Actions -->
            <Border Grid.Row="2" Background="#F8FAFC" Padding="24,16" CornerRadius="0,0,11,11">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="HỦY BỎ" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,12,0" Click="Cancel_Click"/>
                    <Button Content="THÊM DỊCH VỤ" Style="{StaticResource LimeButton}" Width="150" Command="{Binding AddCommand}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
