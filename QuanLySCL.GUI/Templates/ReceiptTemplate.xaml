<UserControl x:Class="QuanLySCL.GUI.Templates.ReceiptTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Background="White" Width="300" d:DesignHeight="600">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel HorizontalAlignment="Center" Margin="0,0,0,10">
            <TextBlock Text="CẦU LÔNG QLSCL" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="ĐC: Số 123, Đường Láng, Hà Nội" FontSize="11" HorizontalAlignment="Center"/>
            <TextBlock Text="ĐT: 0123.456.789" FontSize="11" HorizontalAlignment="Center"/>
            <TextBlock Text="------------------------------------------" FontSize="11" HorizontalAlignment="Center" Margin="0,5"/>
            <TextBlock Text="HÓA ĐƠN THANH TOÁN" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Invoice Info -->
        <Grid Grid.Row="1" Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Số HD:" FontSize="11" Foreground="Gray"/>
            <TextBlock Grid.Column="1" Text="{Binding InvoiceId}" FontSize="11" FontWeight="Bold" HorizontalAlignment="Right"/>

            <TextBlock Grid.Row="1" Text="Ngày:" FontSize="11" Foreground="Gray"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding IssuedAt, StringFormat=dd/MM/yyyy HH:mm}" FontSize="11" HorizontalAlignment="Right"/>

            <TextBlock Grid.Row="2" Text="Khách:" FontSize="11" Foreground="Gray"/>
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding CustomerName}" FontSize="11" HorizontalAlignment="Right"/>
        </Grid>

        <TextBlock Grid.Row="2" Text="------------------------------------------" FontSize="11" HorizontalAlignment="Center" Margin="0,5"/>

        <!-- Details -->
        <ItemsControl Grid.Row="3" ItemsSource="{Binding Items}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="{Binding ServiceName}" FontSize="11" FontWeight="SemiBold"/>
                        <TextBlock Grid.Row="1" Text="{Binding Quantity, StringFormat={}{0} x }" FontSize="10" Foreground="Gray"/>
                        <TextBlock Grid.Row="1" Canvas.Left="30" Text="{Binding Price, StringFormat=N0}" FontSize="10" Foreground="Gray" Margin="30,0,0,0"/>

                        <TextBlock Grid.Column="1" Grid.RowSpan="2" Text="{Binding Total, StringFormat=N0}" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <TextBlock Grid.Row="4" Text="------------------------------------------" FontSize="11" HorizontalAlignment="Center" Margin="0,5"/>

        <!-- Summary -->
        <StackPanel Grid.Row="5" HorizontalAlignment="Right">
            <Grid Margin="0,2" Width="180">
                <TextBlock Text="Thành tiền:" FontSize="11"/>
                <TextBlock Text="{Binding Subtotal, StringFormat=N0}" FontSize="11" HorizontalAlignment="Right"/>
            </Grid>
            <Grid Margin="0,2" Width="180" Visibility="{Binding HasDiscount, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="Giảm giá:" FontSize="11"/>
                <TextBlock Text="{Binding Discount, StringFormat=-N0}" FontSize="11" HorizontalAlignment="Right"/>
            </Grid>
            <Grid Margin="5,5,0,5" Width="180">
                <TextBlock Text="TỔNG CỘNG:" FontSize="13" FontWeight="Bold"/>
                <TextBlock Text="{Binding TotalPayable, StringFormat=N0}" FontSize="13" FontWeight="Bold" HorizontalAlignment="Right"/>
            </Grid>
        </StackPanel>

        <!-- Footer -->
        <StackPanel Grid.Row="6" Margin="0,20,0,0" HorizontalAlignment="Center">
            <TextBlock Text="Cảm ơn quý khách!" FontStyle="Italic" FontSize="11" HorizontalAlignment="Center"/>
            <TextBlock Text="Hẹn gặp lại!" FontStyle="Italic" FontSize="11" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
