<Window x:Class="BadmintonManagement.GUI.ForgotPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Khôi phục mật khẩu" Height="500" Width="400" 
        WindowStartupLocation="CenterScreen" AllowsTransparency="True" 
        WindowStyle="None" Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border CornerRadius="20" Background="White" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.2"/>
        </Border.Effect>

        <Grid>
            <Button x:Name="btnClose" Content="✕" 
                    HorizontalAlignment="Right" VerticalAlignment="Top" 
                    Width="35" Height="35" Margin="0,5,5,0"
                    Background="Transparent" BorderThickness="0" 
                    Foreground="#636e72" FontSize="16" Cursor="Hand"
                    Click="btnClose_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="17">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#fab1a0"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <StackPanel Margin="30">
                <TextBlock Text="🔑" FontSize="35" HorizontalAlignment="Center" Margin="0,5"/>
                <TextBlock Text="KHÔI PHỤC MẬT KHẨU" FontSize="18" FontWeight="Black" Foreground="#0984e3" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <Border x:Name="brdAlert" Background="#fab1a0" CornerRadius="8" Padding="10,8" Margin="0,0,0,15" Visibility="Collapsed">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="⚠️" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="txtAlertMessage" Grid.Column="1" FontSize="11" Foreground="#d63031" TextWrapping="Wrap" FontWeight="SemiBold"/>
                    </Grid>
                </Border>

                <TextBlock Text="Bạn muốn nhận mã qua đâu?" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ComboBox x:Name="cbMethod" Height="35" Margin="0,0,0,15" 
                          VerticalContentAlignment="Center" 
                          SelectedIndex="-1" 
                          SelectionChanged="cbMethod_SelectionChanged">
                    <ComboBoxItem Content="Nhận qua Gmail"/>
                    <ComboBoxItem Content="Nhận qua Số điện thoại"/>
                </ComboBox>

                <StackPanel x:Name="stkInputRecovery" Visibility="Collapsed">
                    <TextBlock x:Name="lblInputTitle" Text="Nhập thông tin:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtInputRecovery" Height="35" Margin="0,0,0,15" VerticalContentAlignment="Center" Padding="10,0"/>
                    <Button x:Name="btnSendOTP" Content="GỬI MÃ XÁC NHẬN" Click="btnSendOTP_Click" Background="#0984e3" Foreground="White" FontWeight="Bold" Height="40" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="20"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>

                <StackPanel x:Name="stkOTP" Visibility="Collapsed" Margin="0,20,0,0">
                    <Separator Margin="0,0,0,20"/>
                    <TextBlock Text="Nhập mã OTP (6 số):" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtOTP" Height="35" Margin="0,0,0,15" VerticalContentAlignment="Center" MaxLength="6" Padding="10,0" TextAlignment="Center" FontSize="16" FontWeight="Bold"/>
                    <Button x:Name="btnVerify" Content="XÁC NHẬN MÃ" Click="btnVerify_Click" Background="#2ecc71" Foreground="White" FontWeight="Bold" Height="40" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="20"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>

                <StackPanel x:Name="stkNewPassword" Visibility="Collapsed" Margin="0,20,0,0">
                    <Separator Margin="0,0,0,20"/>
                    <TextBlock Text="Mật khẩu mới (ít nhất 6 ký tự):" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <PasswordBox x:Name="txtNewPass" Height="35" Margin="0,0,0,15" VerticalContentAlignment="Center" Padding="10,0"/>
                    <Button x:Name="btnUpdatePass" Content="ĐỔI MẬT KHẨU" Click="btnUpdatePass_Click" Background="#e67e22" Foreground="White" FontWeight="Bold" Height="40" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="20"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>

                <Button Content="Quay lại Đăng nhập" Click="btnBack_Click" Background="Transparent" BorderThickness="0" Foreground="#636e72" Margin="0,15,0,0" Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>